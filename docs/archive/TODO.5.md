UI

- Channels shown like a Mixer board.  each Channel's Tab Header:  
  - editable name/description in-place.  also, in the absence of a description, use the name as the description.  enable description editing only when triggered, and when it's erased, hide it \- this implies that the remaining 'name' field stands for both.  
  - have control widgets (ex: a mini-slider for adjusting the specificity)  
  - show status indicators (ex: \# new messages, ping time, etc..)  
  - remove/hide/archive 'x' button: remove it from the active list but keep it in an archive/trash  
    

IEML, Semantic Web ontologies: alternate embedding interfaces  

